# Benchmarks

- `latest/` holds the most recent benchmark run.
- `baseline/` holds the promoted comparison baseline.
- `runs/` holds timestamped historical benchmark runs.

Result files are generated locally and are intentionally ignored by git.
